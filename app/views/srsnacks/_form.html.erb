<%= form_for(@srsnack) do |f| %>
  <% if @srsnack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@srsnack.errors.count, "error") %> prohibited this srsnack from being saved:</h2>

      <ul>
      <% @srsnack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :follow_count %><br>
    <%= f.number_field :follow_count %>
  </div>
  <div class="field">
    <%= f.label :photo %><br>
    <%= f.text_field :photo %>
  </div>
  <div class="field">
    <%= f.label :pchome_url %><br>
    <%= f.text_field :pchome_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
